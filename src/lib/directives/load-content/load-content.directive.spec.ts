import { LoadContentDirective } from './load-content.directive';

describe('LoadContentDirective', () => {
  it('should create an instance', () => {
    const directive = new LoadContentDirective();
    expect(directive).toBeTruthy();
  });
});
